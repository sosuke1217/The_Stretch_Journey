        <li class="nav-item">
          <%= link_to admin_user_path, class: 'nav-link' do %>
            <i class="fa-solid fa-users"></i> 会員一覧
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to admin_genres_path, class: 'nav-link' do %>
            <i class="fa-solid fa-list"></i> ジャンル一覧
          <% end %>
        </li>
<%= form_with(model: @genre, url: admin_genre_path(@genre), method: :patch) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  
  <%= f.submit %>

  <% if @genre.persisted? %>
    <%= link_to '削除', admin_genre_path(@genre), method: :delete, data: { confirm: '本当に削除しますか？' } %>
  <% end %>

<% end %>