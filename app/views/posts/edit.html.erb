<h1>編集画面</h1>
<%= render 'layouts/error_messages', object: @post %>
<%= form_with model: @post, url: post_path(@post), method: :patch do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :title %>
  
  <h4>本文</h4>
  <%= f.text_area :body %>
  
  <%= f.submit '保存' %>
  
<% end %>
